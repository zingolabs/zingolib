
// use tempdir::{self, TempDir};
// use scenarios::setup::ClientManager;

// #[tokio::test]
// async fn test_simple_sync() {
//     let temp_dir = TempDir::new("dwld_test").unwrap();
//     let path = temp_dir.path().to_path_buf();

//     let server_id = zingoconfig::construct_server_uri(Some(format!("http://127.0.0.1:9067")));

//     let client = ClientManager::new(
//             server_id,
//             path,
//             "still champion voice habit trend flight survey between bitter process artefact blind carbon truly provide dizzy crush flush breeze blouse charge solid fish spread"
//         )
//             .build_new_faucet(663150, true).await;
//     // let client = darksidewalletd_client(temp_dir.into_path());

//     // let result = client.do_sync(true).await.unwrap();

//     // assert!(result.has_key("result"));

//     // let res_value = match result {
//     //     json::JsonValue::Object(res) => res.get("result").unwrap_or_else(|| &JsonValue::Boolean(false)).as_bool().unwrap_or(false),
//     //     _ => false
//     // };

//     // assert!(res_value);
// }
